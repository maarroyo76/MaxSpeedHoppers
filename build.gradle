// Version-specific configuration lives in versions/<mc-version>/build.gradle
// Shared Java source is in src/main/java/ (referenced by each subproject)
// Shared assets (icon) are in src/main/resources/ (referenced by each subproject)
